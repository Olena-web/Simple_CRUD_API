import { userId } from '../utils/userId';
import { User} from '../utils/types';

const newUser: User = {
    id: userId,
    username: 'John Doe',
    age: 30,
    hobbies: ['Sports', 'Cooking'],
};

